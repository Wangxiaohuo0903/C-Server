# Server-11：大模型结合服务器初探
## 第二部分：大模型的基础概念

> **学习目标**：理解大模型内部的关键机制——如何将文字转换成数字（Tokenization）、模型参数是什么、为什么需要量化、GGUF文件格式、以及完整的推理流程。

---

## 1. 从文本到数字：Tokenization（分词）

### 1.1 为什么需要分词？

**根本原因**：计算机只认识数字，不认识文字。

```
你看到的：     "Hello, world!"
计算机看到的：  ？？？（无法理解）

需要转换：
"Hello, world!" → [15496, 11, 995, 0]
                  数字序列，计算机可以处理
```

**类比**：摩斯密码

```
文字：   S  O  S
摩斯码： ... --- ...
数字：   [0,0,0] [1,1,1] [0,0,0]

同样的道理：
英文：   "Hello"
Token ID: [15496]
```

### 1.2 分词的基本过程

**完整示例**：

```cpp
// Server-11中的实际代码
std::string text = "Hello, world!";

// 调用llama.cpp的tokenize函数
std::vector<llama_token> tokens = tokenize(vocab, text);

// 结果：
tokens = [15496, 11, 995, 0]
//        Hello   ,  world !
```

**详细步骤**：

```
第1步：文本预处理
输入："Hello, world!"
↓ 统一格式
"Hello, world!"（保持原样，不转小写）

第2步：分词（Tokenization）
"Hello, world!" → ["Hello", ",", " world", "!"]
↓ 注意：
  - "Hello" 是一个完整的token
  - "," 逗号单独是一个token
  - " world" 包含前面的空格
  - "!" 感叹号单独是一个token

第3步：查词汇表（Vocabulary）
词汇表（SmolLM有49152个词）：
  0: "<|endoftext|>"（特殊：结束标记）
  1: "<|im_start|>"（特殊：对话开始）
  ...
  15496: "Hello"       ← 找到了
  11: ","              ← 找到了
  995: " world"        ← 找到了（注意空格）
  0: "!"               ← 可能映射到其他ID

第4步：输出Token序列
[15496, 11, 995, 0]
```

### 1.3 不同的分词策略

#### 策略1：按字符分词（Character-level）

```
文本："Hello"
分词：['H', 'e', 'l', 'l', 'o']
Token IDs: [72, 101, 108, 108, 111]

优点：
✅ 词汇表很小（只需26个字母+标点）
✅ 永远不会遇到"未知词"

缺点：
❌ 序列太长（5个字符 = 5个token）
❌ 失去了词语的完整性
❌ "Hello"和"hell"只差一个字符，但意思完全不同
```

#### 策略2：按单词分词（Word-level）

```
文本："Hello world"
分词：["Hello", "world"]
Token IDs: [15496, 995]

优点：
✅ 序列短（2个token）
✅ 保留词语完整性

缺点：
❌ 词汇表巨大（需要包含所有可能的单词）
❌ 遇到新词就无法处理
   例如："ChatGPT"可能不在词汇表中
```

#### 策略3：BPE（Byte Pair Encoding）⭐ 主流方案

**核心思想**：折中方案——常见词是完整token，罕见词拆分成子词

```
示例1：常见词
文本："Hello"
分词：["Hello"]（完整）
Token ID: [15496]

示例2：罕见词
文本："unhappiness"（不太常见）
分词：["un", "happiness"]（拆分成前缀和词根）
Token IDs: [403, 23871]

示例3：非常罕见的词
文本："antidisestablishmentarianism"（反对废除国教主义）
分词：["anti", "dis", "establish", "ment", "arian", "ism"]
Token IDs: [..., ..., ..., ..., ..., ...]
```

**SmolLM使用的分词器**：

```cpp
// SmolLM词汇表信息
Vocabulary size: 49152
Tokenizer type: BPE (GPT-2 tokenizer)

常见token示例：
Token 374: "is"
Token 362: "2"
Token 10: "+"
Token 30: "?"

特殊token：
Token 0: "<|endoftext|>"  (EOS - End of Sequence)
Token 1: "<|im_start|>"   (对话开始)
Token 2: "<|im_end|>"     (对话结束)
```

### 1.4 实际例子：Server-11中的Tokenization

**代码实现**（SimpleInference.h:110-144）：

```cpp
std::vector<llama_token> tokenize(const llama_vocab* vocab, const std::string& text) {
    // 预分配空间（文本长度 × 4，通常够用）
    std::vector<llama_token> tokens(text.size() * 4);

    // 调用llama.cpp的tokenize函数
    int n_tokens = llama_tokenize(
        vocab,              // 词汇表（49152个词）
        text.c_str(),      // 输入文本
        text.size(),       // 文本长度
        tokens.data(),     // 输出buffer
        tokens.size(),     // buffer大小
        true,              // add_special：添加BOS（开始标记）
        false              // parse_special：不解析特殊token
    );

    if (n_tokens < 0) {
        // buffer不够大，重新分配
        tokens.resize(-n_tokens);
        n_tokens = llama_tokenize(vocab, text.c_str(), text.size(),
                                  tokens.data(), tokens.size(), true, false);
    }

    tokens.resize(n_tokens);
    return tokens;
}

// 使用示例
std::string prompt = "What is 2+2?";
std::vector<llama_token> tokens = tokenize(vocab, prompt);

// 输出tokens的内容
std::cout << "Tokens: [";
for (size_t i = 0; i < tokens.size(); ++i) {
    std::cout << tokens[i];
    if (i < tokens.size() - 1) std::cout << ", ";
}
std::cout << "]\n";

// 输出：Tokens: [1841, 374, 362, 10, 17, 30]
//              What  is   2   +   2   ?
```

**为什么"2+2"会被拆成3个token？**

```
"2+2" 的分词过程：
1. "2"    → Token 362
2. "+"    → Token 10
3. "2"    → Token 362

原因：
- 数字"2"在训练数据中很常见，单独是一个token
- 符号"+"也很常见，单独是一个token
- 如果写"2+2="可能会是4个token：[362, 10, 362, 28]
```

### 1.5 Tokenization的重要性

**影响1：上下文窗口限制**

```
SmolLM的上下文窗口：2048 tokens

例子：
短文本："Hello" = 1 token
       可以处理2048个"Hello"

长文本："The quick brown fox jumps over the lazy dog"
       = 10 tokens
       可以处理约200个这样的句子

中文："你好世界" = ?
      中文通常每个字是1-2个token
      = 4-8 tokens
```

**影响2：生成速度**

```
生成1个token ≈ 0.5秒（CPU推理）

如果用户要求生成100字的文章：
- 英文：约20个token × 0.5秒 = 10秒
- 中文：约50个token × 0.5秒 = 25秒

结论：中文生成比英文慢，因为token更多
```

**影响3：成本计算**

```
很多AI API按token收费：
- OpenAI: $0.002 / 1000 tokens

同样的内容：
英文："Hello world" = 2 tokens
中文："你好世界"     = 4 tokens

中文成本是英文的2倍！
```

---

## 2. 模型参数：神经网络的"知识"

### 2.1 什么是参数？

**类比1：人脑的神经连接**

```
人脑：
├── 860亿个神经元
└── 100万亿个突触（连接）
    ↑ 每个突触有"强度"

大模型：
├── SmolLM: 3.6亿个参数
├── TinyLlama: 11亿个参数
└── GPT-4: 1.76万亿个参数
    ↑ 每个参数是一个小数
```

**类比2：菜谱中的配方**

```
菜谱：
盐：5克      ← 参数1
糖：10克     ← 参数2
酱油：15毫升  ← 参数3

大模型：
参数1: 0.7235
参数2: -0.3421
参数3: 1.5672
...
参数3.6亿: 0.0892
```

### 2.2 参数的本质：权重矩阵

**简化的神经网络**：

```
输入层 → 隐藏层 → 输出层

输入：[x1, x2, x3]
       ↓ ×权重矩阵W1
隐藏：[h1, h2]
       ↓ ×权重矩阵W2
输出：[y1, y2, y3]

权重矩阵W1（3×2）:
[0.5  -0.3]
[0.8   0.2]
[-0.1  0.9]
↑ 6个参数

权重矩阵W2（2×3）:
[0.4  -0.6  0.7]
[0.2   0.5  -0.8]
↑ 6个参数

总参数：6 + 6 = 12个
```

**SmolLM的参数规模**：

```
SmolLM-360M:
├── 嵌入层（Embedding）
│   └── 49152(词汇表) × 960(隐藏维度) = 4700万参数
├── Transformer层（32层）
│   ├── 自注意力层
│   │   ├── Q矩阵：960×960 = 92万参数
│   │   ├── K矩阵：960×960 = 92万参数
│   │   ├── V矩阵：960×960 = 92万参数
│   │   └── O矩阵：960×960 = 92万参数
│   │   小计：368万参数/层
│   ├── 前馈网络（FFN）
│   │   ├── Gate矩阵：960×2560 = 246万参数
│   │   ├── Up矩阵：960×2560 = 246万参数
│   │   └── Down矩阵：2560×960 = 246万参数
│   │   小计：738万参数/层
│   └── 层归一化
│       小计：约2000参数/层
│   总计：(368万+738万+0.2万)×32层 = 3.5亿参数
└── 输出层
    └── 49152×960 = 4700万参数

总计：约3.6亿参数
```

### 2.3 参数如何存储？

**原始格式（FP32）**：

```cpp
// 每个参数是一个32位浮点数
float param1 = 0.7235981;   // 4字节
float param2 = -0.3421008;  // 4字节
float param3 = 1.5672340;   // 4字节

// SmolLM-360M的原始大小
360,000,000 参数 × 4字节 = 1,440,000,000字节
                        = 1.44GB
```

**量化格式（Q4_K）**：

```cpp
// 每个参数用4位整数表示
// 16个参数共享一组缩放因子

原始：
[0.7235, -0.3421, 1.5672, ..., 0.0892]  // 16个FP32 = 64字节

量化：
缩放因子scale: 0.15  // 2字节
量化值：[5, -2, 10, ..., 1]  // 16个4位 = 8字节
总计：2 + 8 = 10字节

压缩率：64字节 → 10字节 = 6.4倍压缩
```

### 2.4 参数规模对比

| 模型 | 参数量 | FP32大小 | Q4大小 | 用途 |
|------|--------|---------|--------|------|
| **SmolLM-360M** | 3.6亿 | 1.4GB | **259MB** | Server-11使用 |
| **TinyLlama-1.1B** | 11亿 | 4.4GB | **638MB** | Server-11可选 |
| Llama2-7B | 70亿 | 28GB | 4GB | 需要GPU |
| Llama2-13B | 130亿 | 52GB | 7.5GB | 需要高端GPU |
| ChatGPT-3.5 | 175亿 | 700GB | 100GB | 云端部署 |
| GPT-4 | 1.76万亿 | 7TB | 1TB | 超大集群 |

**为什么选择SmolLM-360M？**

```
CPU推理能力评估：
├── 内存限制：普通服务器 4-8GB
│   └── SmolLM: 259MB ✅ 可以
│   └── TinyLlama: 638MB ✅ 可以
│   └── Llama2-7B: 4GB ⚠️ 勉强
│
├── 推理速度：
│   └── SmolLM: ~2-3 tokens/s ✅ 可接受
│   └── TinyLlama: ~1-1.5 tokens/s ⚠️ 较慢
│   └── Llama2-7B: ~0.3 tokens/s ❌ 太慢
│
└── 质量：
    └── SmolLM: 基础对话 ✅
    └── TinyLlama: 较好对话 ✅
    └── Llama2-7B: 高质量 ✅

结论：SmolLM最适合Server-11的CPU推理场景
```

---

## 3. 模型量化：压缩技术

### 3.1 为什么需要量化？

**问题**：原始模型太大

```
TinyLlama-1.1B的原始大小（FP32）：
11亿参数 × 4字节/参数 = 4.4GB

问题：
1. 内存占用大
   - 加载模型：4.4GB
   - KV Cache：44MB
   - 中间激活：~500MB
   - 总计：~5GB
   → 很多电脑内存不够

2. 推理速度慢
   - FP32计算慢
   - 内存带宽成为瓶颈
   - CPU Cache不够用

3. 不适合边缘设备
   - 手机、树莓派、嵌入式设备
   - 内存通常<2GB
```

### 3.2 量化的原理

**核心思想**：用更少的位数表示参数

```
FP32（32位浮点数）：
数值范围：-3.4×10³⁸ ~ 3.4×10³⁸
精度：约7位有效数字

示例：
0.7235981 = 01000000 00111010 01011100 10110111
            符号位 指数位(8)  尾数位(23)
            ↑ 32位 = 4字节

↓ 量化到4位

Q4（4位整数）：
数值范围：0-15（无符号）或 -8~7（有符号）
精度：约1位有效数字

示例：
0.7235981 ≈ 7 = 0111
            ↑ 4位 = 0.5字节

压缩率：32位 → 4位 = 8倍压缩
```

**量化方法：对称量化**

```
原始值：[-0.8, -0.3, 0.0, 0.5, 0.9]

第1步：找出最大值
max_val = 0.9

第2步：计算缩放因子
scale = max_val / 7 = 0.9 / 7 ≈ 0.129

第3步：量化
-0.8 / 0.129 ≈ -6 → -6
-0.3 / 0.129 ≈ -2 → -2
 0.0 / 0.129 ≈  0 →  0
 0.5 / 0.129 ≈  4 →  4
 0.9 / 0.129 ≈  7 →  7

量化结果：[-6, -2, 0, 4, 7]  // 每个值4位
缩放因子：0.129  // 额外2字节

第4步：反量化（推理时）
-6 × 0.129 = -0.774
-2 × 0.129 = -0.258
 0 × 0.129 =  0.000
 4 × 0.129 =  0.516
 7 × 0.129 =  0.903

误差：
原始 vs 量化
-0.8  vs -0.774  (误差3.25%)
-0.3  vs -0.258  (误差14%)
 0.5  vs  0.516  (误差3.2%)
 0.9  vs  0.903  (误差0.3%)
```

### 3.3 不同量化级别对比

**Q8（8位量化）**：

```
精度：256个离散值
文件大小：原始的1/4
质量损失：~5%
速度提升：~1.5倍

适用：需要高质量但文件较大的场景
```

**Q4_K（4位量化，K-quant变体）⭐ Server-11使用**：

```
精度：16个离散值
文件大小：原始的1/8
质量损失：~10%
速度提升：~3倍

特点：
- 每16个参数共享一个缩放因子
- 重要的参数用更高精度
- 不重要的参数用更低精度

TinyLlama-1.1B：
- FP32: 4.4GB
- Q4_K: 638MB（压缩6.9倍）
- 质量：几乎感觉不到差异

SmolLM-360M：
- FP32: 1.4GB
- Q4_K: 259MB（压缩5.4倍）
```

**Q2（2位量化）**：

```
精度：4个离散值
文件大小：原始的1/16
质量损失：~30%
速度提升：~5倍

问题：
- 精度太低，模型质量明显下降
- 生成的文本可能不通顺
- 只适合极端资源受限的场景
```

### 3.4 量化的实际效果

**测试场景**：同一个问题用不同量化级别

```
问题："What is the capital of France?"

FP32（原始）：
"The capital of France is Paris. Paris is located in the
 north-central part of France..."
质量：★★★★★

Q8：
"The capital of France is Paris. Paris is located in the
 north-central part of France..."
质量：★★★★★（几乎相同）

Q4_K（Server-11使用）：
"The capital of France is Paris. Paris is located in
 northern France..."
质量：★★★★☆（稍简化，但正确）

Q2：
"The capital France Paris. Paris north France..."
质量：★★☆☆☆（语法错误，但主要信息正确）
```

**结论**：Q4_K是最佳平衡点

```
性能对比（TinyLlama-1.1B，CPU推理）：
├── FP32
│   ├── 文件：4.4GB
│   ├── 内存：~5GB
│   ├── 速度：~0.8 tokens/s
│   └── 质量：100%
│
├── Q8
│   ├── 文件：1.1GB
│   ├── 内存：~1.5GB
│   ├── 速度：~1.2 tokens/s
│   └── 质量：95%
│
├── Q4_K ⭐
│   ├── 文件：638MB
│   ├── 内存：~1.1GB
│   ├── 速度：~1.5 tokens/s
│   └── 质量：90%
│   └── 最佳选择！
│
└── Q2
    ├── 文件：275MB
    ├── 内存：~600MB
    ├── 速度：~2 tokens/s
    └── 质量：70%（不推荐）
```

---

## 4. GGUF格式：模型文件的"容器"

### 4.1 什么是GGUF？

**全称**：GPT-Generated Unified Format

**类比**：就像ZIP文件格式

```
ZIP文件：
myarchive.zip
├── 文件1.txt
├── 文件2.jpg
└── 文件夹/
    └── 文件3.pdf

GGUF文件：
tinyllama-q4.gguf
├── 模型元数据（名称、版本、作者等）
├── 词汇表（49152个词）
├── 模型参数（11亿个数）
└── 配置信息（层数、隐藏维度等）
```

### 4.2 GGUF文件结构

```
tinyllama-q4.gguf (638MB)
├─ [0-3字节] 魔数（Magic Number）
│  └─ 0x46554747 ('GGUF' 的十六进制)
│     用于识别文件类型
│
├─ [4-7字节] 版本号
│  └─ 3 (GGUF v3)
│
├─ [8-15字节] Tensor数量
│  └─ 201 (TinyLlama有201个张量)
│
├─ [16-23字节] 元数据数量
│  └─ 23 (TinyLlama有23个元数据项)
│
├─ [24-...] 元数据区
│  ├─ general.name: "TinyLlama 1.1B"
│  ├─ general.architecture: "llama"
│  ├─ general.quantization_version: 2
│  ├─ general.file_type: 15 (Q4_K)
│  ├─ llama.context_length: 2048
│  ├─ llama.embedding_length: 2048
│  ├─ llama.block_count: 22
│  ├─ llama.feed_forward_length: 5632
│  ├─ llama.attention.head_count: 32
│  ├─ llama.attention.head_count_kv: 4
│  ├─ tokenizer.ggml.model: "llama"
│  ├─ tokenizer.ggml.tokens: [32000个字符串]
│  │   └─ ["<s>", "</s>", "the", "in", ...]
│  ├─ tokenizer.ggml.scores: [32000个浮点数]
│  ├─ tokenizer.ggml.token_type: [32000个整数]
│  ├─ tokenizer.ggml.bos_token_id: 1
│  ├─ tokenizer.ggml.eos_token_id: 2
│  └─ ...
│
└─ [...-638MB] Tensor数据区
   ├─ token_embd.weight
   │  └─ 形状：[32000, 2048], Q4_K, 120MB
   ├─ blk.0.attn_q.weight
   │  └─ 形状：[2048, 2048], Q4_K, 4MB
   ├─ blk.0.attn_k.weight
   │  └─ 形状：[2048, 512], Q4_K, 1MB
   ├─ blk.0.attn_v.weight
   ├─ blk.0.attn_output.weight
   ├─ blk.0.ffn_gate.weight
   ├─ blk.0.ffn_down.weight
   ├─ blk.0.ffn_up.weight
   ├─ blk.1...
   ├─ blk.2...
   ...
   └─ blk.21.ffn_up.weight
```

### 4.3 为什么用GGUF？

**对比其他格式**：

**PyTorch格式（.pth/.bin）**：
```
优点：
✅ PyTorch原生支持
✅ 生态好，工具多

缺点：
❌ 需要Python环境
❌ 文件体积大（通常是FP32）
❌ 加载慢
❌ 依赖PyTorch库（几百MB）
```

**ONNX格式（.onnx）**：
```
优点：
✅ 跨框架（TensorFlow、PyTorch都支持）
✅ 有优化工具

缺点：
❌ 转换复杂
❌ 对大模型支持不完善
❌ 量化支持有限
```

**GGUF格式（.gguf）**：
```
优点：
✅ 专为llama.cpp设计
✅ 原生支持量化（Q2, Q4, Q8等）
✅ 单文件，包含所有信息
✅ 快速加载（mmap支持）
✅ 跨平台（Windows/Linux/Mac）
✅ 零依赖（不需要Python）

缺点：
❌ 只能用llama.cpp
❌ 生态相对小（但快速发展中）
```

### 4.4 GGUF加载过程

**Server-11中的代码**：

```cpp
// SimpleInference.h:33-53
bool loadModel(const std::string& model_path) {
    // 设置模型参数
    llama_model_params model_params = llama_model_default_params();

    // 加载模型文件
    std::cout << "[SimpleInference] Loading model: " << model_path << "\n";
    model_ = llama_load_model_from_file(model_path.c_str(), model_params);

    if (!model_) {
        std::cerr << "[SimpleInference] Failed to load model!\n";
        return false;
    }

    std::cout << "[SimpleInference] Model loaded successfully!\n";
    return true;
}
```

**llama.cpp内部的加载过程**：

```
第1步：打开文件
fopen("tinyllama-q4.gguf", "rb")

第2步：读取文件头
读取魔数：0x46554747 ✅ 确认是GGUF文件
读取版本：3 ✅ 支持v3

第3步：读取元数据
循环读取23个元数据项：
  general.name = "TinyLlama 1.1B"
  llama.context_length = 2048
  tokenizer.ggml.tokens = [32000个词]
  ...

第4步：读取Tensor信息
读取201个Tensor的元数据：
  Tensor #0: token_embd.weight
    - 类型：Q4_K
    - 形状：[32000, 2048]
    - 偏移量：文件中的位置
  Tensor #1: blk.0.attn_q.weight
    ...

第5步：内存映射（mmap）
不是把整个638MB加载到内存，而是：
  mmap(文件描述符, 638MB)
  → 操作系统按需加载
  → 只有访问的部分才真正读入内存

第6步：初始化词汇表
从元数据中提取tokenizer信息
构建Token ID → 文本的映射表

完成！模型已加载，可以开始推理
```

**mmap的优势**：

```
传统加载方式：
1. 分配638MB内存
2. 读取文件到内存
3. 峰值内存：638MB × 2 = 1.3GB

mmap方式：
1. 映射文件到虚拟内存
2. 按需加载（只加载使用的部分）
3. 峰值内存：~700MB（只加载了常用的部分）

优点：
✅ 加载快（不需要读取整个文件）
✅ 内存占用小（未使用的部分不加载）
✅ 多进程共享（操作系统级别优化）
```

---

## 5. 推理过程：从输入到输出

### 5.1 完整流程图

```
用户输入："What is AI?"
    ↓
┌─────────────────────────────────────────┐
│ 第1步：Tokenization（分词）             │
│ "What is AI?" → [1841, 374, 15592, 30]  │
└─────────────────────────────────────────┘
    ↓
┌─────────────────────────────────────────┐
│ 第2步：嵌入层（Embedding）               │
│ Token IDs → 词向量                      │
│ 1841 → [0.23, -0.45, 0.78, ..., 0.12]  │
│        (960维向量)                       │
└─────────────────────────────────────────┘
    ↓
┌─────────────────────────────────────────┐
│ 第3步：Transformer层（32层循环）        │
│ 每层做两件事：                          │
│ ├─ 自注意力（Self-Attention）          │
│ │  └─ 理解词与词之间的关系              │
│ └─ 前馈网络（FFN）                      │
│    └─ 计算特征                          │
└─────────────────────────────────────────┘
    ↓
┌─────────────────────────────────────────┐
│ 第4步：输出层                           │
│ 最后一个位置的隐藏状态                  │
│ → 映射到词汇表大小                      │
│ → 49152个概率值                         │
│                                         │
│ Token 0 ("<|endoftext|>"): 0.001       │
│ Token 1 ("<|im_start|>"): 0.003        │
│ Token 374 ("is"): 0.002                │
│ ...                                     │
│ Token 15592 ("AI"): 0.005              │
│ Token 10840 ("artificial"): 0.85 ⭐    │
│ ...                                     │
└─────────────────────────────────────────┘
    ↓
┌─────────────────────────────────────────┐
│ 第5步：采样（Sampling）                 │
│ 贪婪采样：选择概率最大的token            │
│ 选中：Token 10840 ("artificial")        │
└─────────────────────────────────────────┘
    ↓
┌─────────────────────────────────────────┐
│ 第6步：Detokenization                   │
│ Token 10840 → " artificial"             │
│ (注意前面有空格)                         │
└─────────────────────────────────────────┘
    ↓
┌─────────────────────────────────────────┐
│ 第7步：循环生成                         │
│ "What is AI? artificial" → 继续预测     │
│ → "intelligence"                        │
│ → "."                                   │
│ → 遇到结束标记，停止                     │
└─────────────────────────────────────────┘
    ↓
最终输出："artificial intelligence."
完整回答："What is AI? artificial intelligence."
```

### 5.2 关键步骤详解

#### 步骤1：Tokenization（已在前文详细讲解）

```cpp
// SimpleInference.h:80
std::vector<llama_token> tokens = tokenize(vocab, prompt);
```

#### 步骤2：嵌入层（Embedding）

**作用**：将离散的Token ID转换成连续的向量

```
为什么需要嵌入？
Token ID是离散的数字：1841, 374, 15592
无法直接计算相似度：
  1841和1842很接近吗？不一定！

嵌入向量是连续的：
Token 1841 ("What") → [0.23, -0.45, 0.78, ..., 0.12] (960维)
Token 374 ("is")    → [0.21, -0.43, 0.76, ..., 0.15]
Token 15592 ("AI")  → [0.89, 0.34, -0.67, ..., 0.91]

现在可以计算相似度：
  cos_sim("What", "is") = 0.95（很相似，都是常用词）
  cos_sim("is", "AI") = 0.23（不太相似，意义不同）
```

**实现**：

```cpp
// llama.cpp内部
// token_embd.weight是一个查找表（Lookup Table）
// 形状：[49152, 960]（词汇表大小 × 嵌入维度）

向量 = token_embd.weight[token_id];

// 示例
token_id = 1841;
embedding = token_embd.weight[1841];  // 取出第1841行，共960个数
// [0.23, -0.45, 0.78, ..., 0.12]
```

#### 步骤3：Transformer层

**自注意力（Self-Attention）的作用**：

```
输入序列："The cat sat on the mat"

处理"sat"时，模型需要知道：
- 谁sat？ → 关注"cat"（主语）
- sat在哪里？ → 关注"mat"（地点）

自注意力机制计算每个词对"sat"的重要性：
The   cat   sat   on   the   mat
0.05  0.60  0.10  0.05  0.05  0.15
      ↑最重要                ↑次重要

结果："sat"的表示会融入"cat"和"mat"的信息
```

**数学过程**（简化版）：

```
输入：4个token的嵌入向量
X = [x1, x2, x3, x4]  // 每个xi是960维

第1步：计算Q, K, V
Q = X × W_q  // Query（查询）
K = X × W_k  // Key（键）
V = X × W_v  // Value（值）

第2步：计算注意力分数
Scores = Q × K^T / √d_k
// 结果是4×4的矩阵，表示每对词之间的相关性

第3步：Softmax归一化
Attention = softmax(Scores)
// 每行和为1，表示概率分布

第4步：加权求和
Output = Attention × V
// 根据注意力权重，融合其他词的信息
```

**Server-11中的实际层数**：

```
SmolLM-360M：
├── 32个Transformer层
│   每层包含：
│   ├── 自注意力（Multi-Head Attention）
│   │   └── 15个注意力头（heads）
│   ├── 前馈网络（FFN）
│   │   ├── Gate层：960 → 2560
│   │   ├── Up层：960 → 2560
│   │   └── Down层：2560 → 960
│   └── 层归一化（LayerNorm）×2
│
└── 总计算量：
    每生成1个token：
    - 约7亿次浮点运算（FLOPs）
    - CPU需要~0.5秒
```

#### 步骤4：输出层

**作用**：将隐藏状态映射到词汇表

```cpp
// 输入：最后一个位置的隐藏状态（960维）
hidden_state = [0.12, -0.34, 0.78, ..., 0.45]

// 输出矩阵：[960, 49152]
// 相当于49152个分类器，每个预测一个token的概率
logits = hidden_state × output_weight

// 结果：49152个分数（logits）
logits = [
    -5.23,    // Token 0的分数
    -3.45,    // Token 1的分数
    ...
    8.92,     // Token 10840的分数 ← 最高
    ...
]

// Softmax转换为概率
probs = softmax(logits)
probs = [
    0.0001,   // Token 0: 0.01%
    0.0003,   // Token 1: 0.03%
    ...
    0.8500,   // Token 10840: 85% ← 最高概率
    ...
]
```

#### 步骤5：采样（Sampling）

**贪婪采样（Server-11使用）**：

```cpp
// SimpleInference.h:198-206
int next_token = 0;
float max_prob = logits[0];

for (int i = 1; i < vocab_size; ++i) {
    if (logits[i] > max_prob) {
        max_prob = logits[i];
        next_token = i;
    }
}

// 结果：next_token = 10840（概率最高的token）
```

**其他采样策略**（未在Server-11中实现）：

```
Top-K采样：
从概率最高的K个token中随机选择
例如K=5：
  Token 10840: 85%
  Token 4156: 8%
  Token 9542: 3%
  Token 2134: 2%
  Token 8921: 1%
  从这5个中随机选（概率越高，选中概率越大）

Top-P（核采样）：
选择累积概率达到P的token
例如P=0.9：
  Token 10840: 85%（累积85%）
  Token 4156: 8%（累积93%，超过90%，停止）
  从这2个中随机选

Temperature：
调整概率分布的"锐利度"
  T=0.1：[0.99, 0.01, 0, 0, ...]（接近确定性）
  T=1.0：[0.85, 0.08, 0.03, 0.02, ...]（原始概率）
  T=2.0：[0.45, 0.30, 0.15, 0.08, ...]（更随机）
```

#### 步骤6：Detokenization

```cpp
// SimpleInference.h:215-219
char piece[256];
int len = llama_token_to_piece(
    vocab,
    next_token,      // 10840
    piece,           // 输出buffer
    sizeof(piece),
    0,               // lstrip
    false            // special
);

output.append(piece, len);

// 结果：piece = " artificial"（注意前面有空格）
```

**为什么有空格？**

```
BPE分词器的特点：
很多token包含前导空格

示例：
Token 10840: " artificial"（有空格）
Token 14567: "artificial"（无空格）

用途：
" artificial" 用于词中间或开头
"artificial" 用于词的一部分（如"antiartificial"）
```

#### 步骤7：生成循环

```cpp
// SimpleInference.h:190-236（简化）
std::string output;
int n_past = prompt_tokens.size();  // 已处理的token数

for (int step = 0; step < max_tokens; ++step) {
    // 7.1 获取logits
    const float* logits = llama_get_logits(ctx);

    // 7.2 采样
    int next_token = argmax(logits);

    // 7.3 检查结束
    if (next_token == eos_token) {
        std::cout << "EOS reached!\n";
        break;
    }

    // 7.4 Detokenization
    std::string piece = token_to_text(next_token);
    output += piece;

    // 7.5 准备下一次推理
    llama_batch batch = create_batch({next_token}, n_past);
    llama_decode(ctx, batch);

    n_past++;  // 位置+1
}

return output;
```

**生成示例**：

```
Prompt: "What is AI?"

循环1：
  输入：[1841, 374, 15592, 30]（What is AI ?）
  预测：Token 10840 (" artificial")
  输出：" artificial"
  新序列：[1841, 374, 15592, 30, 10840]

循环2：
  输入：[1841, 374, 15592, 30, 10840]（... artificial）
  预测：Token 11478 (" intelligence")
  输出：" intelligence"
  新序列：[..., 10840, 11478]

循环3：
  输入：[..., 11478]（... intelligence）
  预测：Token 13 (".")
  输出："."
  新序列：[..., 11478, 13]

循环4：
  输入：[..., 13]（... .）
  预测：Token 0 (EOS - End of Sequence)
  结束生成

最终输出：" artificial intelligence."
```

### 5.3 KV Cache优化

**问题**：重复计算

```
不使用Cache：
生成第1个token：
  计算4个token的注意力：Q×K^T
  计算量：4次

生成第2个token：
  需要计算5个token（4个prompt + 1个生成的）
  重新计算前4个token的K和V ← 浪费！
  计算量：5次

生成第3个token：
  计算6个token
  重新计算前5个token ← 更浪费！
  计算量：6次

总计：4 + 5 + 6 + ... + 64 = 2176次计算
```

**解决方案：KV Cache**

```
使用Cache：
生成第1个token：
  计算4个token的K和V
  缓存：KV_cache = [K1, K2, K3, K4]
  计算量：4次

生成第2个token：
  只计算新token的K5和V5
  从缓存读取：[K1, K2, K3, K4]
  新缓存：KV_cache = [K1, K2, K3, K4, K5]
  计算量：1次（只算新的）

生成第3个token：
  只计算K6和V6
  计算量：1次

总计：4 + 1 + 1 + ... + 1 = 4 + 60 = 64次计算

加速比：2176 / 64 = 34倍！
```

**内存代价**：

```
SmolLM-360M的KV Cache：
每个token：
  K: 960维 × 2字节(FP16) = 1.9KB
  V: 960维 × 2字节 = 1.9KB
  总计：3.8KB/token

最大上下文2048 tokens：
  3.8KB × 2048 = 7.8MB

32层Transformer：
  7.8MB × 32 = 250MB

结论：为了34倍加速，花250MB内存是值得的！
```

---

## 小结

本部分详细讲解了大模型的五个核心概念：

1. **Tokenization（分词）**
   - 文字→数字的转换
   - BPE算法：常见词完整，罕见词拆分
   - SmolLM词汇表：49152个token

2. **模型参数**
   - SmolLM：3.6亿个参数
   - 每个参数是一个小数（权重）
   - FP32格式：1.4GB

3. **模型量化**
   - Q4_K：4位整数表示参数
   - 压缩率：~6倍
   - 质量损失：~10%（可接受）

4. **GGUF格式**
   - 单文件包含所有信息
   - 支持量化和mmap加载
   - llama.cpp专用格式

5. **推理流程**
   - Tokenization → Embedding → Transformer → Output → Sampling
   - KV Cache优化：加速34倍
   - 生成循环：逐token预测

**下一部分预告**：llama.cpp推理框架的详细使用——API讲解、代码示例、版本兼容性问题等。

---

**继续阅读**：[第三部分：llama.cpp推理引擎](./第三部分-llama.cpp推理引擎.md)
